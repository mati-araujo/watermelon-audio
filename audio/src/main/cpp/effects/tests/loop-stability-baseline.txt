# Efectos cuya RESPUESTA AL IMPULSO CRECE en vez de decaer, con los parametros
# DE FABRICA, medido por `LoopStability.EveryDeclaredCaseStillReproduces`.
#
# Que mide esto y por que no lo mide `nyquist-baseline.txt`: aquello atribuye al
# SAMPLE RATE, y esta clase de defecto no depende del rate. Un lazo de
# realimentacion con ganancia mayor que 1 crece igual a 8 kHz que a 96, y lo que
# cambia con el rate es solo cuantas muestras tarda en verse.
#
# ESTO ES UN TRINQUETE. El test falla si:
#   - un efecto que NO esta aca empieza a crecer  (deuda nueva), y TAMBIEN
#   - un efecto que SI esta aca deja de crecer    (deuda pagada: borrar linea)
#
# Misma semantica que `reset-baseline.txt`, `nyquist-baseline.txt` y
# `scripts/rt-safety-baseline.txt`.
#
# Formato:  <EFFECT_TYPE> | <razon de crecimiento por ventana de 0,5 s> | <por que>
#
# ---------------------------------------------------------------------------
# VACIO DESDE WD-3.6. Tuvo una sola entrada, `SPRING_REVERB`, y se pago.
#
# QUE ERA: el camino cruzado del tanque multiplicaba `feedback` por una suma de
# taps que ya valia 1,49 (0,55 + 0,42 + 0,30 + 0,22), y el drip sumaba
# 0,45 * drip MAS, por afuera del feedback. Con el decay por defecto (2,2 ->
# feedback 0,692) la ganancia daba 1,031 antes de contar el drip: **el efecto era
# inestable con los valores de fabrica**, a todo sample rate.
#
# COMO SE PAGO: el lazo pasa a repartirse un PRESUPUESTO de ganancia en vez de
# que cada camino traiga la suya, y el drip descuenta de ese mismo presupuesto.
# Acotado por construccion, no por un clamp al final. Medido sobre las 126
# combinaciones de las tres perillas (decay x drip x tension): **cero crecen**,
# y la peor razon es 0,5734. El RT60 sigue siendo monotono en el recorrido del
# decay —1,0 s en el minimo, 3,0 s en el maximo— que es lo que separa este
# arreglo del arreglo trivial de bajar el feedback hasta que no explote.
#
# DE DONDE SALIO: no lo encontro este test, lo encontro el diagnostico de
# WD-3.5. `SPRING_REVERB` figuraba en `nyquist-baseline.txt` como "diverge a
# 8.000 Hz", y la medicion mostro que divergia a los OCHO rates probados con el
# mismo tiempo. Aparecia solo en el mas bajo porque la ventana del test iba en
# BLOQUES: 32 bloques de 512 son 2,0 s a 8 kHz y 0,34 s a 48 kHz.
# **Un baseline indexado por sample rate atribuye al sample rate.**
#
# La regla, para el proximo que agregue una entrada a CUALQUIERA de estos
# archivos: corre el repro FUERA del eje que el baseline indexa. Si tambien
# falla ahi, no es de ese archivo.
# ---------------------------------------------------------------------------
